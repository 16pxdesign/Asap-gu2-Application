@model List<Application.Models.ActivitiesViewModel>


    <div class="card mt-3">
        <div class="card-body">
            @foreach (var item in Model)
            {
            <dl class="row">
                <dt class="col-sm-2">
                    <label>@item.Name</label>
                </dt>

            </dl>
            }
        </div>
    </div>

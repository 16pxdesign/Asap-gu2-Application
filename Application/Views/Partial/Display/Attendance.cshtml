@model List<MemberViewModel>


    <div class="card mt-3">
        <div class="card-body">
        @foreach (var item in Model)
        {
            <dl class="row">

                <dd class="col-sm-3">
                    @item.Fullname
                </dd>

            </dl>
        }
        </div>
    </div>
